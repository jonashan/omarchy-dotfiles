# Personal shell aliases. Edit this file directly — it is symlinked to
# ~/.bash_aliases, so changes apply in the next shell you open. No need to
# re-run setup.sh unless you delete the symlink.
#
# Sourced after Omarchy's own aliases, so anything here wins on a name clash.
# See Omarchy's defaults with: cat "$OMARCHY_PATH/default/bash/aliases"

# --- Tools ---
alias lg='lazygit'
alias ld='lazydocker'

# --- Projects ---
alias te='cd ~/Work/TeamEffect/'
alias te2='cd ~/Work/teameffect-v2/'
